@model EcoHotels.Web.UI.Areas.Admin.Models.ResetAccountModel
@{
    ViewBag.Title = "logon";
    Layout = "~/Areas/Admin/Views/Shared/_Basic.cshtml";
}

@section javascript{
    <script type="text/javascript">
        $(document).ready(function () {
            $('input[name=email]').focus();
        });
    </script>
}

    <style type="text/css">
      /* Override some defaults */
      html, body {
        background-color: #eee;
      }
      body {
        padding-top: 40px;
      }
      .container {
        width: 300px;
        margin: 150px auto 100px;
      }
 
      /* The white background content wrapper */
      .container > .content {
        background-color: #fff;
        padding: 20px;
        margin: 0 -20px;
        -webkit-border-radius: 10px 10px 10px 10px;
           -moz-border-radius: 10px 10px 10px 10px;
                border-radius: 10px 10px 10px 10px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.15);
           -moz-box-shadow: 0 1px 2px rgba(0,0,0,.15);
                box-shadow: 0 1px 2px rgba(0,0,0,.15);
      }
 
      .login-form, reset-form {
        margin-left: 65px;
        margin-right: 36px;
      }
      
      #reset{ display: none; }
 
      legend {
        margin-right: -50px;
        font-weight: bold;
        color: #404040;
      }
 
    </style>


    <div class="container">
        <div class="content">
            <div class="row">
                <div id="login" class="login-form">
                    <h2>Reset password</h2>

                    @if (ViewData["error"] != null)
                    {
                        <div class="alert alert-error"><p>@ViewData["error"]</p></div>
                    }
                    
                    @if (ViewData["success"] != null)
                    {
                        <div class="alert alert-success"><p>@ViewData["success"]</p></div>
                    }

                    <form class="form" action="@Url.Action("reset", "account", null)" method="post">
                        <div>
                            @Html.AntiForgeryToken()
                        </div>
                        <fieldset>
                            <div class="clearfix">
                                <input name="email" type="text" placeholder="Email">
                            </div>
                            
                            <div style="text-align: right;">
                                <a href="@Url.Action("logon", "account", null)" style="margin-right: 8px;">Logon</a>
                                <button class="btn btn-primary" type="submit">Reset</button>
                            </div>                     
                        </fieldset>
                    </form>
                </div>
            </div>
        </div>
    </div> 